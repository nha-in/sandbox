services:
  docs:
    image: sandbox_local_docs
    container_name: sandbox_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - path: ./.envs/.local/.django
        required: false
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./sandbox:/app/sandbox:z
    ports:
      - '9000:9000'
    command: /start-docs
