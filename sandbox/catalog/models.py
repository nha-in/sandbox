"""Catalog: the LGD reference data the portal reads.

Milestones used to live here as rows. They are defined by the workflow in code
now (`programmes.abdm.Milestone`), so nothing in this app owns them.
"""

from __future__ import annotations
