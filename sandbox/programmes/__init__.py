"""Programmes: each module declares one programme's workflows in code.

The lockdown rule (plan/09-redesign.md §2): which workflows exist, their
states, transitions, forms and completion rules live here and change only by
code deploy. Nothing in this package reads or writes the database.
"""
